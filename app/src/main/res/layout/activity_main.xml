<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_vertical_margin"
    android:paddingRight="@dimen/activity_vertical_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/colorPrimaryDark"
    tools:context="codectech.login.MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/imageView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="2"

            app:srcCompat="@drawable/relooking" />

        <TextView
            android:id="@+id/textView2"
            style="@style/textView_styles"
            android:layout_margin="5dp"
            android:text="adedora" />

        <EditText
            android:id="@+id/editText"
            android:hint="Username"
            style="@style/editText_styles"
            android:inputType="textEmailAddress" />

        <EditText
            android:id="@+id/editText2"
            android:hint="Password"
            style="@style/editText_styles"
            android:inputType="textPassword" />

        <Button
            android:id="@+id/button"
            style="@style/button_styles"
            android:text="log in" />

        <TextView
            android:id="@+id/textView"
            style="@style/textView_styles"
            android:text="Forget your pssword?" />
    </LinearLayout>
</RelativeLayout>
